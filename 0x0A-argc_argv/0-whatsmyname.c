#include <stdio.h>

/**
 * main - the code to print name
 * @argc: the argument counter
 * @argv: the array of the argument
 * Return: 0 on success
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
